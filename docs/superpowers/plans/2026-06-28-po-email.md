# Purchase Order Email Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a "Enviar al proveedor" button on confirmed PO detail pages that emails the purchase order to the supplier with a PDF attachment.

**Architecture:** New `apps/purchases/email.py` mirrors `apps/sales/email.py`. Email template in `templates/purchases/email/` reuses existing sales partials unchanged (`_items_table.html`, `_totals.html`, `_footer.html`, etc.). A POST-only `PurchaseOrderEmailView` guards status and delegates to the email function. Button renders in the overflow section of the PO detail template, CONFIRMED-only.

**Tech Stack:** Django 4.x, `EmailMultiAlternatives`, WeasyPrint (optional PDF), pytest + factory_boy, Bootstrap 5 + Bootstrap Icons.

---

## File Map

| File | Action | Responsibility |
|------|--------|----------------|
| `apps/purchases/email.py` | **Create** | `send_purchase_order_email()` + `_po_pdf_bytes()` |
| `templates/purchases/email/purchase_order_email.html` | **Create** | HTML email body for PO |
| `apps/purchases/tests/test_email.py` | **Create** | Tests for email function |
| `apps/purchases/views/purchase_orders.py` | **Modify** | Add `PurchaseOrderEmailView` at end of file |
| `apps/purchases/views/__init__.py` | **Modify** | Export `PurchaseOrderEmailView` |
| `apps/purchases/urls.py` | **Modify** | Add `po_email` URL + import |
| `templates/purchases/purchase_order_detail.html` | **Modify** | Add email button in overflow + dropdown |

---

## Task 1: Email function + failing tests

**Files:**
- Create: `apps/purchases/tests/test_email.py`
- Create: `apps/purchases/email.py`

- [ ] **Step 1: Write the failing tests**

Create `apps/purchases/tests/test_email.py`:

```python
from unittest.mock import patch, MagicMock
import pytest
from apps.purchases.tests.factories import (
    PurchaseDocumentFactory,
    PurchaseDocumentItemFactory,
    SupplierFactory,
)
from apps.accounts.tests.factories import OrganizationFactory


@pytest.mark.django_db
def test_send_po_email_returns_true_when_supplier_has_email(mailoutbox):
    org = OrganizationFactory()
    supplier = SupplierFactory(organization=org, email="proveedor@empresa.com")
    po = PurchaseDocumentFactory(
        organization=org,
        supplier=supplier,
        doc_type="PURCHASE_ORDER",
        status="CONFIRMED",
        number="OC-00001",
    )
    PurchaseDocumentItemFactory(purchase_document=po)
    request = MagicMock()

    from apps.purchases.email import send_purchase_order_email
    with patch("apps.purchases.email._po_pdf_bytes", return_value=None):
        result = send_purchase_order_email(po, request)

    assert result is True
    assert len(mailoutbox) == 1
    assert mailoutbox[0].to == ["proveedor@empresa.com"]
    assert "OC-00001" in mailoutbox[0].subject


@pytest.mark.django_db
def test_send_po_email_returns_false_when_no_supplier_email(mailoutbox):
    org = OrganizationFactory()
    supplier = SupplierFactory(organization=org, email="")
    po = PurchaseDocumentFactory(
        organization=org,
        supplier=supplier,
        doc_type="PURCHASE_ORDER",
        status="CONFIRMED",
    )
    request = MagicMock()

    from apps.purchases.email import send_purchase_order_email
    result = send_purchase_order_email(po, request)

    assert result is False
    assert len(mailoutbox) == 0


@pytest.mark.django_db
def test_send_po_email_attaches_pdf_when_weasyprint_available(mailoutbox):
    org = OrganizationFactory()
    supplier = SupplierFactory(organization=org, email="proveedor@empresa.com")
    po = PurchaseDocumentFactory(
        organization=org,
        supplier=supplier,
        doc_type="PURCHASE_ORDER",
        status="CONFIRMED",
        number="OC-00042",
    )
    PurchaseDocumentItemFactory(purchase_document=po)
    request = MagicMock()

    fake_pdf = b"%PDF-1.4 fake"
    from apps.purchases.email import send_purchase_order_email
    with patch("apps.purchases.email._po_pdf_bytes", return_value=fake_pdf):
        send_purchase_order_email(po, request)

    assert len(mailoutbox) == 1
    attachments = mailoutbox[0].attachments
    assert len(attachments) == 1
    name, content, mime = attachments[0]
    assert name == "orden_OC-00042.pdf"
    assert content == fake_pdf
    assert mime == "application/pdf"
```

- [ ] **Step 2: Run tests — expect ImportError (module doesn't exist yet)**

```bash
pytest apps/purchases/tests/test_email.py -v
```

Expected: `ModuleNotFoundError: No module named 'apps.purchases.email'`

- [ ] **Step 3: Create `apps/purchases/email.py`**

```python
import mimetypes
import base64

from django.conf import settings
from django.core.mail import EmailMultiAlternatives
from django.template.loader import render_to_string
from django.utils.translation import gettext as _

from apps.sales.email import _logo_url, _signature_url, _pdf_bytes

from .models import PurchaseDocument


def _po_pdf_bytes(po: PurchaseDocument, request) -> bytes | None:
    return _pdf_bytes(
        "purchases/purchase_order_print.html",
        {
            "po": po,
            "items": po.items.all(),
            "org": po.organization,
        },
        request,
    )


def send_purchase_order_email(po: PurchaseDocument, request) -> bool:
    """Send PO to supplier. Returns True if sent, False if no supplier email."""
    to_email = po.supplier.email
    if not to_email:
        return False
    org = po.organization
    ctx = {
        "po": po,
        "items": po.items.all(),
        "org": org,
        "logo_url": _logo_url(org, request),
        "sender": request.user,
        "sender_signature_url": _signature_url(request.user, request),
    }
    html_body = render_to_string(
        "purchases/email/purchase_order_email.html", ctx, request=request
    )
    doc_ref = po.number or _("Borrador")
    subject = f"Orden de Compra {doc_ref} – {org.name}"
    plain = (
        f"Orden de Compra {doc_ref}\n"
        f"Total: {po.total}\n\n"
        "Revise este correo en un cliente compatible con HTML."
    )
    msg = EmailMultiAlternatives(
        subject, plain, settings.DEFAULT_FROM_EMAIL, [to_email]
    )
    msg.attach_alternative(html_body, "text/html")
    pdf = _po_pdf_bytes(po, request)
    if pdf:
        msg.attach(f"orden_{doc_ref}.pdf", pdf, "application/pdf")
    msg.send(fail_silently=False)
    return True
```

- [ ] **Step 4: Run tests — expect TemplateDoesNotExist (template missing, created in Task 2)**

```bash
pytest apps/purchases/tests/test_email.py -v
```

Expected: `django.template.exceptions.TemplateDoesNotExist: purchases/email/purchase_order_email.html`

---

## Task 2: Email template

**Files:**
- Create: `templates/purchases/email/purchase_order_email.html`

- [ ] **Step 1: Create the template directory and file**

Create `templates/purchases/email/purchase_order_email.html`:

```html
{% load i18n humanize %}<!DOCTYPE html>
<html lang="es">
<head>
{% include "sales/partials/_email_head.html" %}
  <title>{% trans "Orden de Compra" %} {{ po.number|default:"Borrador" }} – {{ org.name }}</title>
</head>
<body bgcolor="#f4f4f4">

<!-- Preheader -->
<div style="display:none;max-height:0;overflow:hidden;mso-hide:all;font-size:1px;line-height:1px;color:#f4f4f4;">{% trans "Orden de Compra" %} {{ po.number|default:"Borrador" }} · {% trans "Total" %}: {{ po.total|floatformat:2|intcomma }}</div>

<!-- Wrapper -->
<table role="presentation" width="100%" cellpadding="0" cellspacing="0" bgcolor="#f4f4f4">
  <tr>
    <td align="center" style="padding: 24px 16px;">

      <!-- Card -->
      <table role="presentation" width="620" cellpadding="0" cellspacing="0" style="max-width:620px;width:100%;background:#ffffff;border-radius:8px;overflow:hidden;box-shadow:0 2px 12px rgba(0,0,0,0.08);">

        {% include "sales/partials/_topbar.html" %}

        {% trans "Orden de Compra" as doc_title %}
        {% include "sales/partials/_org_header.html" with doc_title=doc_title doc_number=po.number|default:"Borrador" %}

        <!-- ── GREETING ── -->
        <tr>
          <td style="padding: 20px 28px 0;">
            <p style="margin:0;font-size:14px;color:#374151;line-height:1.6;">
              {% trans "Estimado(a)" %} <strong style="color:#1a1a1a;">{{ po.supplier.contact_name|default:po.supplier.name }}</strong>,
            </p>
            <p style="margin:8px 0 0;font-size:13px;color:#6b7280;line-height:1.65;">
              {% trans "Adjunto encontrará la orden de compra correspondiente a los artículos indicados. Quedo a su disposición para cualquier consulta o ajuste." %}
            </p>
          </td>
        </tr>

        <!-- ── PARTIES (Proveedor | Comprador) ── -->
        <tr>
          <td style="padding: 18px 28px 0;">
            <table role="presentation" width="100%" cellpadding="0" cellspacing="0">
              <tr>
                <!-- Supplier (recipient) -->
                <td width="50%" valign="top" style="padding-right:8px;">
                  <table role="presentation" width="100%" cellpadding="0" cellspacing="0">
                    <tr>
                      <td style="background:#f8f9fa;border-left:3px solid #1e2130;border-radius:0 4px 4px 0;padding:10px 12px;">
                        <div style="font-size:10px;text-transform:uppercase;letter-spacing:0.8px;color:#6b7280;font-weight:700;margin-bottom:4px;">{% trans "Proveedor" %}</div>
                        <div style="font-weight:700;font-size:13px;color:#1a1a1a;">{{ po.supplier.name }}</div>
                        {% if po.supplier.rnc_cedula %}<div style="font-size:11px;color:#555;margin-top:2px;">{{ po.supplier.get_id_type_display }}: {{ po.supplier.rnc_cedula }}</div>{% endif %}
                        {% if po.supplier.address %}<div style="font-size:11px;color:#555;margin-top:2px;">{{ po.supplier.address }}</div>{% endif %}
                      </td>
                    </tr>
                  </table>
                </td>
                <!-- Org (buyer) -->
                <td width="50%" valign="top" style="padding-left:8px;">
                  <table role="presentation" width="100%" cellpadding="0" cellspacing="0">
                    <tr>
                      <td style="background:#f8f9fa;border-left:3px solid #1e2130;border-radius:0 4px 4px 0;padding:10px 12px;">
                        <div style="font-size:10px;text-transform:uppercase;letter-spacing:0.8px;color:#6b7280;font-weight:700;margin-bottom:4px;">{% trans "Comprador" %}</div>
                        <div style="font-weight:700;font-size:13px;color:#1a1a1a;">{{ org.name }}</div>
                        {% if org.tax_id %}<div style="font-size:11px;color:#555;margin-top:2px;">RNC: {{ org.tax_id }}</div>{% endif %}
                        {% if org.address %}<div style="font-size:11px;color:#555;margin-top:2px;">{{ org.address }}</div>{% endif %}
                      </td>
                    </tr>
                  </table>
                </td>
              </tr>
            </table>
          </td>
        </tr>

        <!-- ── META ── -->
        <tr>
          <td style="padding: 14px 28px 0;">
            <table role="presentation" width="100%" cellpadding="0" cellspacing="0">
              <tr>
                <td style="border-bottom:1px solid #e5e7eb;padding-bottom:12px;">
                  <table role="presentation" cellpadding="0" cellspacing="0">
                    <tr>
                      {% trans "Fecha emisión" as l %}
                      {% include "sales/partials/_meta_item.html" with label=l value=po.issue_date|date:"d/m/Y" pr=24 pl=0 %}
                      {% if po.expected_date %}
                      {% trans "Fecha esperada" as l %}
                      {% include "sales/partials/_meta_item.html" with label=l value=po.expected_date|date:"d/m/Y" value_color="#0369a1" pr=24 pl=0 %}
                      {% endif %}
                      {% if po.currency != "DOP" %}
                      <td style="padding-left:24px;">
                        <div style="font-size:10px;text-transform:uppercase;letter-spacing:0.6px;color:#6b7280;font-weight:700;">{% trans "Moneda" %}</div>
                        <div style="font-weight:700;font-size:12px;color:#1a1a1a;margin-top:2px;">{{ po.currency }} @ {{ po.exchange_rate }}</div>
                      </td>
                      {% endif %}
                    </tr>
                  </table>
                </td>
              </tr>
            </table>
          </td>
        </tr>

        {% include "sales/partials/_items_table.html" %}

        {% include "sales/partials/_totals.html" with doc=po %}

        {% include "sales/partials/_notes.html" with doc=po %}

        <!-- ── SENDER SIGNATURE ── -->
        {% if sender %}
        <tr>
          <td style="padding: 20px 28px 0;">
            <table role="presentation" width="100%" cellpadding="0" cellspacing="0">
              <tr>
                <td style="border-top:1px solid #e5e7eb;padding-top:14px;">
                  <div style="font-size:10px;text-transform:uppercase;letter-spacing:0.6px;color:#6b7280;font-weight:700;margin-bottom:6px;">{% trans "De parte de" %}</div>
                  {% if sender_signature_url %}
                  <img src="{{ sender_signature_url }}" alt="{{ sender.full_name }}" height="48"
                       style="max-height:48px;max-width:160px;display:block;margin-bottom:6px;">
                  {% endif %}
                  <div style="font-size:13px;font-weight:700;color:#1a1a1a;">{{ sender.full_name }}</div>
                  <div style="font-size:11px;color:#6b7280;margin-top:2px;">{{ sender.email }}</div>
                  {% if org.phone %}<div style="font-size:11px;color:#6b7280;margin-top:2px;">{{ org.phone }}</div>{% endif %}
                </td>
              </tr>
            </table>
          </td>
        </tr>
        {% endif %}

        {% include "sales/partials/_footer.html" %}

      </table>
      <!-- /Card -->

    </td>
  </tr>
</table>

</body>
</html>
```

- [ ] **Step 2: Run tests — all three should pass**

```bash
pytest apps/purchases/tests/test_email.py -v
```

Expected:
```
PASSED apps/purchases/tests/test_email.py::test_send_po_email_returns_true_when_supplier_has_email
PASSED apps/purchases/tests/test_email.py::test_send_po_email_returns_false_when_no_supplier_email
PASSED apps/purchases/tests/test_email.py::test_send_po_email_attaches_pdf_when_weasyprint_available
```

- [ ] **Step 3: Commit**

```bash
git add apps/purchases/email.py apps/purchases/tests/test_email.py templates/purchases/email/purchase_order_email.html
git commit -m "feat: add send_purchase_order_email function and template"
```

---

## Task 3: View, URL, exports

**Files:**
- Modify: `apps/purchases/views/purchase_orders.py` (append `PurchaseOrderEmailView`)
- Modify: `apps/purchases/views/__init__.py` (add export)
- Modify: `apps/purchases/urls.py` (add import + path)

- [ ] **Step 1: Add `PurchaseOrderEmailView` to `apps/purchases/views/purchase_orders.py`**

At the top of the file, add `send_purchase_order_email` to the existing imports block (after the existing `from ..services import PurchaseOrderService` line):

```python
from ..email import send_purchase_order_email
```

Then append at the **end** of `apps/purchases/views/purchase_orders.py`:

```python
class PurchaseOrderEmailView(ERPBaseViewMixin, View):
    required_module = "purchasing"

    def post(self, request, pk):
        po = get_object_or_404(
            PurchaseDocument.purchase_orders, pk=pk, organization=request.organization
        )
        if po.status != PurchaseDocument.Status.CONFIRMED:
            messages.warning(request, _("Solo se puede enviar una orden confirmada."))
            return redirect("purchases:po_detail", pk=po.pk)
        try:
            sent = send_purchase_order_email(po, request)
            if sent:
                messages.success(
                    request,
                    _("Correo enviado a %(email)s.") % {"email": po.supplier.email},
                )
            else:
                messages.warning(request, _("El proveedor no tiene correo registrado."))
        except Exception as exc:
            messages.error(
                request,
                _("No se pudo enviar el correo: %(error)s") % {"error": str(exc)},
            )
        return redirect("purchases:po_detail", pk=po.pk)
```

- [ ] **Step 2: Export from `apps/purchases/views/__init__.py`**

In `apps/purchases/views/__init__.py`, add `PurchaseOrderEmailView` to the `purchase_orders` import block:

```python
from .purchase_orders import (
    PurchaseOrderListView,
    PurchaseOrderCreateView,
    PurchaseOrderDetailView,
    PurchaseOrderUpdateView,
    PurchaseOrderConfirmView,
    PurchaseOrderReceiveView,
    PurchaseOrderCancelView,
    PurchaseOrderDeleteView,
    PurchaseOrderCloneView,
    PurchaseOrderPrintView,
    PurchaseOrderEmailView,        # ← add this line
)
```

- [ ] **Step 3: Add URL to `apps/purchases/urls.py`**

In `apps/purchases/urls.py`, add `PurchaseOrderEmailView` to the import list:

```python
from .views import (
    # ...existing imports...
    PurchaseOrderPrintView,
    PurchaseOrderEmailView,        # ← add this line
    # ...rest of imports...
)
```

Then add the URL in the `# ── Purchase Orders ──` section, after the `po_print` line:

```python
path("purchases/purchase-orders/<uuid:pk>/email/",   PurchaseOrderEmailView.as_view(),   name="po_email"),
```

- [ ] **Step 4: Run full test suite to confirm no regressions**

```bash
pytest apps/purchases/ -v
```

Expected: all existing tests pass, no import errors.

- [ ] **Step 5: Commit**

```bash
git add apps/purchases/views/purchase_orders.py apps/purchases/views/__init__.py apps/purchases/urls.py
git commit -m "feat: add PurchaseOrderEmailView and po_email URL"
```

---

## Task 4: PO detail template button

**Files:**
- Modify: `templates/purchases/purchase_order_detail.html`

- [ ] **Step 1: Add email button to overflow section**

In `templates/purchases/purchase_order_detail.html`, locate the `doc-action-overflow` div (around line 57). It currently has buttons for Imprimir and Clonar. Add the email form **after the Imprimir `<a>` tag and before the Clonar `<form>`**:

```html
{% if po.status == "CONFIRMED" %}
<form method="post" action="{% url 'purchases:po_email' po.pk %}">
  {% csrf_token %}
  <button type="submit" class="btn btn-outline-secondary btn-sm">
    <i class="bi bi-envelope me-1"></i>{% trans "Enviar al proveedor" %}
  </button>
</form>
{% endif %}
```

- [ ] **Step 2: Add email item to "Más" dropdown**

In the same file, locate the `doc-action-more` dropdown `<ul>` (around line 96). It has items for Imprimir and Clonar. Add the email form **after the Imprimir `<li>` and before the Clonar `<li>`**:

```html
{% if po.status == "CONFIRMED" %}
<li>
  <form method="post" action="{% url 'purchases:po_email' po.pk %}">
    {% csrf_token %}
    <button type="submit" class="dropdown-item">
      <i class="bi bi-envelope me-2"></i>{% trans "Enviar al proveedor" %}
    </button>
  </form>
</li>
{% endif %}
```

- [ ] **Step 3: Run full test suite**

```bash
pytest apps/purchases/ -v
```

Expected: all tests pass.

- [ ] **Step 4: Commit**

```bash
git add templates/purchases/purchase_order_detail.html
git commit -m "feat: add email button to PO detail page (CONFIRMED only)"
```

---

## Verification

After all tasks complete, manually verify:

1. Confirm a DRAFT PO → status becomes CONFIRMED → "Enviar al proveedor" button appears in overflow and "Más" dropdown.
2. Supplier has email → click button → success toast with email address.
3. Supplier has no email → click button → warning toast.
4. DRAFT PO detail page → no email button visible.
5. Email subject contains PO number and org name.
